class Product {
    constructor(tenSP, gia, screen, backCamera, frontCamera, hinhAnh, mota, type) {
      this.tenSP = tenSP;
      this.price = price;
      this.screen = screen;
      this.backCamera = backCamera;
      this.frontCamera = frontCamera;
      this.hinhAnh = hinhAnh;
      this.mota = mota;
      this.type = type;
    }
  }